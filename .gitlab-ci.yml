deploy to dev.toycode.org:
  script:
  - "npm install"
  - "bower install"
  - "forever stop kidscoding || true"
  - "forever start --uid \"kidscoding\" -a -c \"gulp\" ."
  type: deploy
  tags:
  only:
  - master
